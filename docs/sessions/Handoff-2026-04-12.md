# Session Handoff — 2026-04-12

## What Was Accomplished

### Committed to main (d9ba644)
1. **Initial repo commit** — 34 files, 2660 insertions. Scaffolded FastAPI core (`src/main.py`, `src/config.py`), SvelteKit cyberdeck UI scaffold, deploy assets, BOM spreadsheet, framework docs (`.claude-config/`, `CLAUDE.md`).

### Deployed (operational state, not source code)
1. **Pi 5 boot migrated SD → NVMe** — Cloned via `rpi-clone nvme0n1`. `BOOT_ORDER=0xf416` (NVMe → SD → USB → loop). SD retained as passive fallback.
2. **FastAPI service deployed** — `/home/cypherklein/apollo/` on Pi. Systemd unit at `/etc/systemd/system/apollo.service`, enabled, auto-starts at boot. `GET /health` returns 200 after cold reboot with zero manual intervention.

### Key Discoveries
- **Pi Imager silently lowercases usernames.** User intended `cypherKlein`, actual Linux account is `cypherklein`. Caused a long password-rejection diagnosis. Saved to `project_hardware_status.md`.
- **Pi 5 firmware from Nov 2025 already supports NVMe boot out of the box.** The Session 6 memory note about needing SD-first EEPROM update didn't apply. Memory has been corrected.
- **`BOOT_ORDER` reads right-to-left.** `0xf416` = NVMe(6) → SD(1) → USB(4) → loop(f).
- **`rpi-eeprom-config` shows cached `blconfig`, not the EEPROM write.** Post-write reads look stale until reboot applies the new config. Non-obvious; caused a "did it stick?" detour.
- **`deploy/apollo.service` in the repo is a stale template** assuming user `apollo` at `/opt/apollo` with hardening directives. Diverges from the hand-written service actually deployed (user `cypherklein` at `/home/cypherklein/apollo`, no hardening). Flagged for next session.

## Action Items for Next Session

### Priority
1. **Deploy workflow upgrade** — replace `scp -r` with `git pull` on the Pi OR rsync with `--exclude`. `scp -r` pulls stale `__pycache__` dirs and has no exclude support. Short, unblocks every future code change.
2. **Reconcile `deploy/apollo.service` template with reality.** Update template to match deployed config (or formally document the `/opt/apollo` target as future-state and leave template as aspirational).
3. **First real endpoint — T0 keyword dispatcher + `/dispatch` route.** First real Apollo behavior beyond `/health`. Validates tool-first architecture before layering on LLM fallback.

### Parked
1. **STT on device** — `faster-whisper` + mic + transcribe endpoint. Larger scope; Pi 5 performance validation involved. Queue after dispatcher exists.
2. **SSH key auth on Pi** — currently password-only. Setting up keys would let Claude run commands non-interactively from Windows.
3. **Halo glasses + ESP32 ring integration** — awaiting hardware readiness.

## Branch State
- **main** (local): 1 commit (`d9ba644`) + pending changes for this session's push (skills, session docs, CLAUDE.md update)
- **origin/main**: does not exist yet — this session's push is the first

## Files Changed This Session
| File | Change |
|------|--------|
| `.claude-config/PROJECT_CONTEXT.md` | NEW in `d9ba644` |
| `.claude-config/PROJECT_CORE.md` | NEW in `d9ba644` |
| `.env.example` | NEW in `d9ba644` |
| `.gitignore` | NEW in `d9ba644` |
| `Apollo_BOM_Purchasing.xlsx` | NEW in `d9ba644` |
| `CLAUDE.md` | NEW in `d9ba644`; UPDATE — added Phase 1 Deployment section + repo URL |
| `OrderedPhase1.JPG` | NEW in `d9ba644` |
| `cyberdeck-ui/**` (15 files) | NEW in `d9ba644` — SvelteKit scaffold |
| `deploy/PI_IMAGE_CONFIG.md` | NEW in `d9ba644` |
| `deploy/apollo.service` | NEW in `d9ba644` — stale template, flagged for reconciliation |
| `deploy/setup.sh` | NEW in `d9ba644` |
| `requirements.txt` | NEW in `d9ba644` |
| `src/main.py`, `src/config.py`, `src/**/__init__.py` | NEW in `d9ba644` |
| `.claude/skills/**` | NEW — user-added project skills |
| `docs/sessions/Handoff-2026-04-12.md` | NEW — this document |
| `docs/sessions/Work-Report-2026-04-12.md` | NEW — session narrative |
| `docs/sessions/Actionable-Items.md` | NEW — rolling priority list |
